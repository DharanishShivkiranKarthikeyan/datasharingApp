{
  "name": "dcrypt-web",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build:wasm": "cd rust && wasm-pack build --target web --out-dir ../web/pkg",
    "build": "npm run build:wasm && vite build",
    "serve": "vite preview --port=8080"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "firebase": "^11.4.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "vite": "^5.4.14"
  }
}
